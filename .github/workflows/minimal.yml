name: Example for minimal setup
on:
  push:
    branches:
      - master

jobs:
  linux:
    name: Testi
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v1

    - name: Install packages
      run: ls -la
